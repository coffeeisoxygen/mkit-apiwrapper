[project]
name = "mkit-apiwrapper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi-guard>=4.0.3",
    "fastapi[standard]>=0.116.1",
    "loguru>=0.7.3",
    "loguru-config>=0.1.0",
    "sqlalchemy>=2.0.43",
]
